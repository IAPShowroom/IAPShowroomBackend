/**
 * Test file for jest test cases for the showroomProxy functions.
 */

const showroomProxy = require('../Database/showroomProxy.js');

test('Test createEvents', () => {
    
    showroomProxy.createEvents();
});